A simple JavaScript embedding tool
==================================

This simple tool converts any JavaScript input into a form that can be
trivially embedded into any CommonMark, HTML, or XHTML document, without
changing its meaning.
